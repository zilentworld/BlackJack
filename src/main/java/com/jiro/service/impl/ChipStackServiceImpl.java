package com.jiro.service.impl;

import com.jiro.model.Chip;
import com.jiro.model.PlayerChipStack;
import com.jiro.service.ChipStackService;

public class ChipStackServiceImpl implements ChipStackService {

    public void exchangeChips(PlayerChipStack playerChipStack, Chip fromChip, Chip toChip, int numberToExchange) {
//        if(playerChipStack.contains(fromChip)) {
            //21 stack of 25
            //25 -> 100
            //exchangeChips(player, 25, 100, 8)
//        }
    }

    public void convertToHighestChips(PlayerChipStack playerChipStack, int amount) {

    }
}
