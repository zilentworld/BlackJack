<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans-3.0.xsd">

    <bean id="PlayerMap" class="java.util.concurrent.ConcurrentHashMap"/>
    <bean id="CacheService" class="com.jiro.cache.impl.CacheServiceImpl">

        <property name="playerMap" ref="PlayerMap"/>

    </bean>

    <bean id="RmiPlayerService" class="com.jiro.rmi.impl.RmiPlayerServiceImpl">
        <property name="cacheService" ref="CacheService"/>
    </bean>

    <!-- RMI Server Declaration -->
    <bean class="org.springframework.remoting.rmi.RmiServiceExporter">

        <!-- serviceName represents RMI Service Name -->
        <property name="serviceName" value="RmiPlayerService"/>

        <!-- service represents RMI Object(RMI Service Impl) -->
        <property name="service" ref="RmiPlayerService"/>

        <!-- serviceInterface represents RMI Service Interface exposed -->
        <property name="serviceInterface" value="com.jiro.rmi.RmiPlayerService"/>

        <!-- defaults to 1099 -->
        <property name="registryPort" value="1099"/>

    </bean>

</beans>